name: First Flow
on: push
jobs:
    first_job:
        name: first one
        runs-on: ubuntu-latest
        steps:
           - name: Checkout Action
             uses: actions/checkout@v6
             
           - name: run hello
             run: echo "Hello"

           - name: list files
             run: ls -al

           - name: test if curl is present
             run: curl --help